<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Memória interna -->
    <external-path
        name="external_storage"
        path="." />

    <!-- Android/data -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- Cache externo -->
    <external-cache-path
        name="external_cache"
        path="." />

    <!-- Armazenamento interno do app -->
    <files-path
        name="files"
        path="." />

    <!-- Cache interno do app -->
    <cache-path
        name="cache"
        path="." />

</paths>